# LLMux Environment Configuration
# Copy this file to ~/.config/llmux/env and fill in your secrets
#
#   mkdir -p ~/.config/llmux
#   cp llmux.env.example ~/.config/llmux/env
#   chmod 600 ~/.config/llmux/env

# Required: Tailscale auth key
# Get one from: https://login.tailscale.com/admin/settings/keys
# Use a reusable key for persistent deployments
TS_AUTHKEY=tskey-auth-XXXXXXXXXXXX-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Required: Anthropic OAuth token for headless mode
# Generate using: python cli.py --setup-token
ANTHROPIC_OAUTH_TOKEN=sk-ant-oat01-...

# Optional: Custom Tailscale hostname (default: llmux)
# Your proxy will be available at https://<hostname>.<tailnet>.ts.net
#TS_HOSTNAME=llmux

# Optional: Override default model
#DEFAULT_MODEL=claude-sonnet-4-5-20250929
